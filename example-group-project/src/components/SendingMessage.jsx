const SendingMessage = () => {
  //LOGIC FOR OPTIMISTICALLY RENDERING SENT MESSAGE, & POSTING TO API
  return (
    <form>
      <input></input>
      <button>Send</button>
    </form>
  );
};

export default SendingMessage;
